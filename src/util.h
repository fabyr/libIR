#include "standard.h"
#include <stdint.h>

void complex_mul_i(IR_COMPLEX_T a, IR_COMPLEX_T b);

void complex_add_i(IR_COMPLEX_T a, IR_COMPLEX_T b);

void complex_mul_real_i(IR_COMPLEX_T a, IR_FLOAT_T b);

int32_t pos_modulo(int32_t value, uint32_t m);